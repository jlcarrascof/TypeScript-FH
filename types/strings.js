"use strict";
(() => {
    var _a;
    const batman = 'Batman';
    const greenLantern = "Green Lantern";
    const blackVulcano = `Hero: Black Vulcano`;
    console.log(`I'm ${batman}`);
    console.log(batman.toUpperCase());
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || `It's not present`);
})();
